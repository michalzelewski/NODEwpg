const height = 10;
const base = 4;
const perimeter = 2 * (height + base);
const area = height * base;
const result = `perimeter of rectangle with side a= ${height} and side b= ${base} is ${perimeter}
area of rectangle with side a= ${height} and side b= ${base} is ${area}`;

console.log(result);