const firstNumber = 10;
const secondNumber = 5;

if (firstNumber > secondNumber) {
    console.log(`Number ${firstNumber} is bigger than ${secondNumber}`);
}

// if second number will be bigger than first code won't be execute