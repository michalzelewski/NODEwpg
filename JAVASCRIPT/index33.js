let userCount = 100;
userCount += 5;

console.log(`users count: ${userCount}`);

userCount -= 3;

console.log(`users count: ${userCount}`);

userCount *= 2;

console.log(`users count: ${userCount}`);

//menCount = userCount *= 0.5;
//womenCount = userCount *= 0.5;
//console.log('users count:', menCount);

let menCount = userCount * 0.5;
let womenCount = userCount * 0.5;

console.log(`users count: ${userCount}, ${menCount}, ${womenCount}`);